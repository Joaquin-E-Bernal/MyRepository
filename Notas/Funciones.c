Funciones:
// BUENAS PRACTICA: uso de prototipos, es decir, declarar la funcion con sus parametros, luego viene el main y al final la definicmos.
int nombre(int arg1, int arg2){
	
	int variable;
	variable= arg1-arg2;
		
	return variable;
}
/*
Parametro:

Se especifica el tipo de dato que sera tomado como argumento/parametro dentro de la funcion.
Las variables con las que se quieran operar deben ingresarse de forma ordenada.
 
 
return:

valor que retorna la funcion.
El valor de la variable que se va a devolver, este debe ser del mismo tipo en el que se espcifico la funcion.

*/


intercambioValores()
{
	a=2; b=3;
	
	a=a*b;
	
	b=a/b;
	
	a=a/b;
}

